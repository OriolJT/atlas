apiVersion: v1
kind: Service
metadata:
  name: worker-service
  namespace: atlas
  labels:
    app: worker-service
    app.kubernetes.io/part-of: atlas
spec:
  selector:
    app: worker-service
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
  type: ClusterIP
