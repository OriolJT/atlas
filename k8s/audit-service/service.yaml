apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: atlas
  labels:
    app: audit-service
    app.kubernetes.io/part-of: atlas
spec:
  selector:
    app: audit-service
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
  type: ClusterIP
