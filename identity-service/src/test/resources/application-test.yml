spring:
  flyway:
    schemas: identity
    default-schema: identity
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: identity
